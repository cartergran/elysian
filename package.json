{
  "name": "panorama",
  "version": "0.3",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "install": "npm install --prefix client && npm install --prefix server",
    "build": "npm run build --prefix client",
    "start": "node server/index.js",
    "heroku-postbuild": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cartergran/panorama.git"
  },
  "engines": {
    "node": ">=22 <23"
  }
}
